{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Debug unit tests in library 'csrf'",
      "cargo": {
        "args": ["test", "--no-run", "--lib", "--package=csrf"],
        "filter": {
          "name": "csrf",
          "kind": "lib"
        }
      },
      "args": [],
      "cwd": "${workspaceFolder}",
      "sourceLanguages": ["rust"]
    }
  ]
}
